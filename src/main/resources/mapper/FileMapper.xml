<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.now.core.file.domain.FileMapper">

    <insert id="saveFile" parameterType="File" useGeneratedKeys="true" keyProperty="fileIdx">
        INSERT INTO tb_file (saved_file_name, original_file_name, file_extension, file_size, member_post_idx)
        VALUES (#{savedFileName}, #{originalFileName.originalFileName}, #{fileExtension.fileExtension}, #{fileSize.fileSize}, #{memberPostIdx});
    </insert>

</mapper>