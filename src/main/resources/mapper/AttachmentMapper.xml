<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.now.core.attachment.domain.AttachmentMapper">

    <insert id="saveAttachment" parameterType="Attachment" useGeneratedKeys="true" keyProperty="attachmentIdx">
        INSERT INTO tb_attachment (saved_attachment_name, original_attachment_name, attachment_extension, attachment_size, member_post_idx)
        VALUES (#{savedAttachmentName}, #{originalAttachmentName.originalAttachmentName}, #{attachmentExtension.attachmentExtension}, #{attachmentSize.attachmentSize}, #{memberPostIdx});
    </insert>

    <!-- ThumbNail의 저장 쿼리 -->
    <insert id="saveThumbNail" parameterType="Attachment">
        INSERT INTO tb_thumbnail (member_post_idx, attachment_idx)
        VALUES (#{memberPostIdx}, #{attachmentIdx})
    </insert>


</mapper>